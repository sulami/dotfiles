[user]
name = "Robin Schroer"
email = "git@sulami.xyz"

[signing]
behavior = "own"
backend = "ssh"
key = "~/.ssh/id_ed25519.pub"

[ui]
default-command = "status"
diff-formatter = ":git"
pager = [ "delta", "--pager", "less -FRX" ]

[revsets]
log = "all()"
